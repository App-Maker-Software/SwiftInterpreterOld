    import XCTest
    @testable import SwiftInterpreter

    final class SwiftInterpreterTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(SwiftInterpreter().text, "Hello, World!")
        }
        func testExample2() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(SwiftInterpreter().text, "Hello, World fail!")
        }
    }

    final class SwiftInterpreterTests2: XCTestCase {
        func testExample2_a() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(SwiftInterpreter().text, "Hello, World!")
        }
    }
